# fallback.md (placeholder)

In production, this file stores entries that haven't been classified yet.
The memory routing system writes uncertain entries here, then attempts async LLM classification.

See `src/memory_routing.py` fallback routing logic for details.
